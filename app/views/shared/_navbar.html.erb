<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Home", root_path, class: "navbar-brand" %> 
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
      </ul>

      <ul class="navbar-nav ms-auto mb-2 mb-log-0">
        <% if Current.user.present? %>
          <li class="nav-item">
            <%= button_to "Logout", sessions_destroy_path, method: :delete,  class: "btn btn-outline-success me-2" %>
          </li>
          <li class="nav-item">
            <%= link_to "Посещаемость", example_input_path,  class: "btn btn-outline-success me-2" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", root_path,  class: "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_path,  class: "nav-link"%>
          </li>
        <% end %>
      </ul>

    </div>
  </div>
  </div>

</nav>



