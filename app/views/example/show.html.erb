<div>
  <table class="table">

    <thead>
      <tr>
        <th>№</th>
        <th>Результат</th>
      </tr>
    </thead>
    <tbody>
        <% @result1.each_with_index do |el, idx| %>
          <tr>
            <td> <%= idx %>.</td>
            <td> <%= el %> </td>
          </tr>
        <% end %>
      </tr>
    </tbody>
  </table>
</div>

<%= link_to 'Back', "/" %>
